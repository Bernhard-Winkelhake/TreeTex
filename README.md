# TreeTex
## Information
TreeTex allows users to create, edit, and visualize trees through an graphical interface. Using a treeview, users can easily modify the structure by adding and removing nodes. The editor automatically generates LaTeX code for the tree, which can be directly used in LaTeX documents.

It supports various tree types, such as decision trees, hierarchical trees, and family trees.
